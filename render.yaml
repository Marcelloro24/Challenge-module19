services:
  - type: web
    name: jate-text-editor
    env: node
    buildCommand: |
      npm install
      cd client && npm install
      cd ../server && npm install
      cd .. && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production 